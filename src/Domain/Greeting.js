// Not used in the scope of this example. It's just an example of where domain objects live.
// Their step definitions are not dependent on tags or mock/real situations. They do not use GlobalObjects.
class Greeting {
  constructor(id, owner, content);
}

module.exports = Greeting;
